/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // ✅ App Router is default in Next 13+ (no appDir flag needed)
  // ✅ Keep config minimal for best compatibility/performance
};

module.exports = nextConfig;